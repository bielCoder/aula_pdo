<?php 	
		
		require_once('crud_pdo_connection.php');
			// Deletando dados por passagem de parametro.
		// $res = $connection -> prepare("DELETE FROM crudpdo WHERE id = :id");
		// $id = 5;
		// $res->bindparam(":id",$id);
		// $res->execute(); 
		// Deletando dados sem passagem de parametro.
		// $res = $connection->query("DELETE FROM crudpdo WHERE id = '8'");
 ?>